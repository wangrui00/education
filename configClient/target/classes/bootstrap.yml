spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://user:123@localhost:18083  #配置服务器增加了安全认证，这里需要提供用户名密码
      profile: dev   #对应spring.profiles.active
      label: master #分支名。当配置服务器是git时，默认是master
      username: user
      password: 123